<% content_for(:controllers_for_main) { "form" } %>

<%= render partial: "heading", locals: { board: @board, topic: @topic, posts: @posts } %>

<%= render "shared/lightbox" %>

<%= render partial: "topic_body" %>

<%= render partial: "footing" %>
